import { PageShell } from "@/components/PageShell";

export default function MichelleLayout({ children }: { children: React.ReactNode }) {
  return <PageShell theme="michelle">{children}</PageShell>;
}
